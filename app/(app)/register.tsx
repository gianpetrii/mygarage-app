import { View } from 'react-native';

/** Pantalla dummy para el tab central; la acción real abre QuickRegisterSheet. */
export default function RegisterTabPlaceholder() {
  return <View className="flex-1 bg-background" />;
}
